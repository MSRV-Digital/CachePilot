# Redis Manager Frontend Environment Variables
# Copy this file to .env for local development

# API Base URL
# Development: Use /api (proxied by Vite to avoid CORS)
# Production: Use full API URL (e.g., https://api.example.com)
VITE_API_BASE_URL=/api

# API Key for authentication
# SECURITY: Generate a secure API key using: redis-mgr api key generate
# IMPORTANT: Never commit .env file with real API keys to version control
# Example: VITE_API_KEY=abc123xyz789...
VITE_API_KEY=

# Application Settings
VITE_APP_NAME=Redis Manager
VITE_REFRESH_INTERVAL=30000

# Security Settings
# IMPORTANT: Only set to 'true' in development environments
VITE_DEBUG_MODE=false
